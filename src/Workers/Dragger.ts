import { Camera } from '../Core/Camera.js';
import { Measure } from '../Core/Measure.js';
import { Selector } from './Selector.js';

// Move dragging logic/data/state to this file to clean up App.ts eventually

function DragNote(
  x: number,
  y: number,
  msr: Measure,
  selector: Selector,
  cam: Camera): boolean {
    return false;
  }

