import { Camera } from '../Core/Camera.js';
import { Measure } from '../Core/Measure.js';
import { Page } from '../Core/Page.js';
import { ConfigSettings } from '../Types/Config.js';
declare function RenderPage(page: Page, canvas: HTMLCanvasElement, context: CanvasRenderingContext2D, cam: Camera, formatting: boolean, config: ConfigSettings, measures: Measure[]): void;
export { RenderPage };
//# sourceMappingURL=Page.Renderer.d.ts.map